#!/bin/python

nimm sys

# take in the first argument
zu_lang = masse(sys.argv[1]) > 5

wenn zu_lang == Nein:
  druck("die schnur ist nicht zu lang")
sonst:
  druck("aber jetzt doch ein bisschen kurzer bitte")
